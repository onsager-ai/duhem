# AC-1 — Every listed row has a positive quantity

Apply the same validation to every row in the supplied list. One invalid
row fails the check, and each row retains its own assertion outcomes.
An empty list is inconclusive because no rows were verified.
