verification: Named locator catalog — first consumer
criteria:
  - id: AC-1
    description: A shared locator can address the fixture from one leaf.
    checks:
      - id: AC-1.1
        steps:
          - uses: ui/navigate
            with: { url: "data:text/html,<h1>Catalog Fixture</h1>" }
          - uses: ui/assert-element
            with: { locator: $pages.fixture.heading, expected: visible }
