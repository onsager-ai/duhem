# Duhem root manifest — TODO stub
#
# Generated by `duhem init --pattern B`. Pattern B (per
# `docs/duhem-spec.md` §10.4) co-locates per-feature Verification
# Definitions under a parent directory that carries a root
# manifest aggregating them.
#
# The root manifest schema is still under design — see the
# `spec(schema): root manifest v0.1` spec. Until it lands,
# this file is a placeholder so the directory layout is in
# place; `duhem run` against it will fail until the loader is
# wired. Run the per-feature `duhem.yml` directly in the
# meantime.
#
# When the manifest spec lands, `duhem init --pattern B`'s
# template updates in lockstep; existing stubs need a manual
# rewrite. See `CHANGELOG.md` at that time for migration notes.

# verifications:
#   - {{LEAF}}/duhem.yml
